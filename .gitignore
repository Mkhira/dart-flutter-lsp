# macOS
.DS_Store

# Dart/Flutter build artifacts (regenerated by `pub get` / the analyzer)
.dart_tool/
.packages
build/
pubspec.lock

# Local-only: the internal build brief (not shipped)
DART_FLUTTER_LSP_PLUGIN_BRIEF.md
